{
  "name": "echoshield-relay-server",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon cloud relay and responder console for EchoShield.",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.50.1"
  }
}
